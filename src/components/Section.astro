---
export interface Props {
	title: string;
    src: string;
    text: string;
    pclass: string;
}

const {title, src, text, pclass} = Astro.props;
---
<section class={pclass+" lg:mb-20 lg:flex lg:items-center lg:justify-center my-2 lg:my-5"}>
    <img src={src} class="sliding-image off-screen-left transition-all duration-500 w-full h-auto max-w-xs"/>
    <div class="mt-1 lg:mt-0 lg:ml-4">
        <h2 class="text-3xl font-semibold lg:text-4xl mb-1">{title}</h2>
        <p class="text-lg lg:text-2xl">{text}</p>
    </div>
</section>