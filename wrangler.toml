name = "rodfer-web"
pages_build_output_dir = "dist"
compatibility_date = "2026-05-01"

# KV namespace backing the availability status. Cloudflare Pages reads this
# binding straight from wrangler.toml — no separate dashboard binding needed.
[[kv_namespaces]]
binding = "AVAILABILITY"
id = "8d990070e9a941db847a8d97852666b1"
